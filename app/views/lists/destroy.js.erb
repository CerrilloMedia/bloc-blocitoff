$("#lists").html('<%= j render partial: "lists", locals: {user: @user, lists: @lists } %>');
$('#lists-quantity .panel-body').html('<%= pluralize(@lists.count, "list") %> for <%= @list.user.email %>');
$('#list_title').focus();
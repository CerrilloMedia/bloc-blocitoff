$("#items").html('<%= j render partial: "items/items", locals: { user: @user, items: @list.items } %>');
$('#items-quantity').html('<%= pluralize(@list.items.count, "item") %>');
$('#item_body').val('');